# 이 파일을 복사해 `.env`로 저장한 뒤 값을 조정하세요. (`cp .env.example .env`)
# 백엔드 루트 URL (끝 슬래시 없이). `/api/patients`, `/api/auth` 등은 코드에서 붙습니다.
# 비워 두면 기본 배포 API(https://vibe-pts-checkup-backend.onrender.com)를 사용합니다.

# 다른 백엔드 호스트를 쓸 때만 설정하세요.
# VITE_API_BASE=https://your-api.example.com
